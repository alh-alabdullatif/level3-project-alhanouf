apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: cart-build-taskrun
spec:
  serviceAccountName: alhanouf-sec
  taskRef:
    name: cart-build-task
  params:
  - name: IMAGE
    value: "alhanouf1/carts-build"
  resources:
    inputs:
    - name: git-resource
      resourceRef:
        name: git-resource
